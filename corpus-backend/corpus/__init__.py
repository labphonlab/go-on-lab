"""Go-on Lab corpus backend — research- and commercial-grade speech collection.

The core (models, audio QC, prompt store, pipeline, storage) is pure Python
standard library. Heavy ML capabilities (ASR verification, forced alignment)
are pluggable interfaces with honest baseline implementations.
"""

__version__ = "0.1.0"
