anthropic>=0.40.0
PyYAML>=6.0
cmudict>=1.0.24    # analysis/neighborhood.py — pronouncing dictionary for ND computation
wordfreq>=3.0.0    # analysis/priority.py — real word-frequency data (FL), Apache-2.0 licensed

# input format extraction (analysis/extract.py) — only needed for the
# formats actually used; each is imported lazily so plain .md/.txt input
# works without any of these installed.
striprtf>=0.0.26   # .rtf
python-docx>=1.1.0 # .docx
pypdf>=4.0.0       # .pdf
python-pptx>=0.6.23 # .pptx
